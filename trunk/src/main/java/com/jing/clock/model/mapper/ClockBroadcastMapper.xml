<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jing.clock.model.dao.ClockBroadcastMapper">

<!-- Result Map-->
<resultMap id="ClockBroadcastResultMap" type="com.jing.clock.model.entity.ClockBroadcast">
	<id column="broadcast_id" property="broadcastId"/>
	<result column="store_id" property="storeId"/>
	<result column="stauts" property="stauts"/>
	<result column="types" property="types"/>
	<result column="content" property="content"/>
	<result column="created_by" property="createdBy"/>
	<result column="created_date" property="createdDate"/>
	<result column="updated_by" property="updatedBy"/>
	<result column="updated_date" property="updatedDate"/>
</resultMap>

<sql id="sql_clockBroadcast_item">
	broadcast_id, store_id, stauts, types, content, created_by, created_date, updated_by, updated_date
</sql>
       
<!-- 查询条件 无WHERE -->
<sql id="sql_clockBroadcast_query_nowhere">
	<trim prefix=" " prefixOverrides="AND|OR">
		<if test="clockBroadcast.broadcastId != null">AND broadcast_id = #{clockBroadcast.broadcastId} </if>
		<if test="clockBroadcast.storeId != null">AND store_id like concat(#{clockBroadcast.storeId}, '%') </if>
		<if test="clockBroadcast.stauts != null">AND stauts = #{clockBroadcast.stauts} </if>
		<if test="clockBroadcast.types != null">AND types = #{clockBroadcast.types} </if>
		<if test="clockBroadcast.content != null">AND content like concat(#{clockBroadcast.content}, '%') </if>
		<if test="clockBroadcast.createdBy != null">AND created_by like concat(#{clockBroadcast.createdBy}, '%') </if>
		<if test="clockBroadcast.updatedBy != null">AND updated_by like concat(#{clockBroadcast.updatedBy}, '%') </if>
	</trim>
</sql>
   
<!-- 插入 报钟播报 -->
<insert id="addClockBroadcast" parameterType="com.jing.clock.model.entity.ClockBroadcast" >
	<selectKey keyProperty="broadcastId" order="AFTER" resultType="java.lang.Integer">
		SELECT LAST_INSERT_ID()
	</selectKey>
 	INSERT INTO tc_clock_broadcast 
	<trim prefix=" (" suffix=")" suffixOverrides=",">
		<if test="broadcastId != null"> broadcast_id,</if>
		<if test="storeId != null"> store_id,</if>
		<if test="stauts != null"> stauts,</if>
		<if test="types != null"> types,</if>
		<if test="content != null"> content,</if>
		<if test="createdBy != null"> created_by,</if>
		created_date,
		<if test="updatedBy != null"> updated_by,</if>
		updated_date,
	</trim>	 	
	<trim prefix="values (" suffix=")" suffixOverrides=",">
		<if test="broadcastId != null"> #{broadcastId},</if>
		<if test="storeId != null"> #{storeId},</if>
		<if test="stauts != null"> #{stauts},</if>
		<if test="types != null"> #{types},</if>
		<if test="content != null"> #{content},</if>
		<if test="createdBy != null"> #{createdBy},</if>
		now(),
		<if test="updatedBy != null"> #{updatedBy},</if>
		now(),
	</trim>
</insert>

<!-- 根据标识，修改 报钟播报 -->  
<update id="modifyClockBroadcast" parameterType="com.jing.clock.model.entity.ClockBroadcast" >
  	UPDATE tc_clock_broadcast SET
	<trim suffixOverrides=",">
		<if test="storeId != null"> store_id = #{storeId},</if>
		<if test="stauts != null"> stauts = #{stauts},</if>
		<if test="types != null"> types = #{types},</if>
		<if test="content != null"> content = #{content},</if>
		<if test="createdBy != null"> created_by = #{createdBy},</if>
		<if test="updatedBy != null"> updated_by = #{updatedBy},</if>
		updated_date = now(),
	</trim>
	WHERE broadcast_id = #{broadcastId}
</update>
 
<!-- 删除 报钟播报 -->
<delete id="dropClockBroadcastByBroadcastId" parameterType="java.lang.Integer">
	DELETE FROM tc_clock_broadcast WHERE broadcast_id = #{broadcastId}
</delete> 

<!-- 根据标识查询 报钟播报 -->
<select id="queryClockBroadcastByBroadcastId"  resultMap="ClockBroadcastResultMap" parameterType="java.lang.Integer">
	SELECT 
	<include refid="sql_clockBroadcast_item"/>
	FROM tc_clock_broadcast WHERE broadcast_id = #{broadcastId}
</select>

<!-- 根据属性查询报钟播报 -->
<select id="queryClockBroadcastByProperty"  resultMap="ClockBroadcastResultMap" parameterType="java.util.Map">
	SELECT 
	<include refid="sql_clockBroadcast_item"/>
	FROM tc_clock_broadcast 
	<where>
		<include refid="sql_clockBroadcast_query_nowhere"/>
	</where>
</select>

<!-- 根据报钟播报属性与分页信息分页查询报钟播报信息 -->
<select id="queryClockBroadcastForPage"  resultMap="ClockBroadcastResultMap" > 
	SELECT 
	<include refid="sql_clockBroadcast_item"/>
	FROM tc_clock_broadcast 
	<where>
		<include refid="sql_clockBroadcast_query_nowhere"/>
	</where>
</select>

</mapper>   
